using Avalonia.Controls;

namespace GitVault.App.Views;

/// <summary>Repository-scoped page. Contains view wiring only.</summary>
internal sealed partial class BranchesView : UserControl
{
    public BranchesView() => InitializeComponent();
}
