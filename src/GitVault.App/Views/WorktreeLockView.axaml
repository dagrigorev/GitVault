<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="using:GitVault.App.Markup"
             x:Class="GitVault.App.Views.WorktreeLockView">
  <StackPanel Spacing="0">

    <Grid ColumnDefinitions="110,*" RowDefinitions="Auto">
      <TextBlock Grid.Column="0" VerticalAlignment="Center"
                 Text="{loc:Tr Worktrees_Field_Reason}" />
      <TextBox Grid.Column="1" Text="{Binding Reason, Mode=TwoWay}"
               AutomationProperties.Name="{loc:Tr Worktrees_Field_Reason}" />
    </Grid>

    <Border Classes="note">
      <TextBlock TextWrapping="Wrap" Text="{Binding NoteCaption}" />
    </Border>

  </StackPanel>
</UserControl>
